import { ListItem } from '@mui/material';
import styled from '@emotion/styled';

export const IssueItem = styled(ListItem)`
  width: 100%;
  height: 100px;
  display: flex;
  flex-direction: row;
  align-items: center;
  justify-content: space-between;
  border-bottom: 1px solid #e1e4e8;
`;
